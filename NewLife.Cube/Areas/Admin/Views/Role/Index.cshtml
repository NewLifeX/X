@model XCode.EntityList<Role>
@{
    var grid = ViewBag.Grid;
}
<div class="panel panel-default">
    <div class="table-responsive">
        <table class="table table-bordered table-hover table-striped table-condensed">
            <thead>
                <tr>
                    <th style="width: 50px;"> <a href="@Url.Action("Index/Sort/id")">编号</a></th>
                    <th style="width: 120px;"><a href="@Url.Action("Index/Sort/Name")">名称</a></th>
                    <th style="width: 120px;"><a href="@Url.Action("Index/Sort/IsSystem")">系统角色</a></th>
                    <th style="width: 120px;"><a href="@Url.Action("Index/Sort/Permission")">权限</a></th>
                    <th>操作</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var entity in Model)
                {
                    <tr>
                        <td>@entity.ID</td>
                        <td>@entity.Name</td>
                        <td>@entity.IsSystem</td>
                        <td>@entity.Permission</td>
                        <td>
                            <i class="glyphicon glyphicon-edit"></i>
                            <a href="@Url.Action("Form/" + @entity.ID)">编辑</a>
                            <i class="glyphicon glyphicon-remove"></i>
                            <a href="@Url.Action("Delete/" + @entity.ID)">删除</a>
                        </td>
 </tr>
                }
            </tbody>
        </table>
    </div>
    <div class="panel-footer">
        <p>
            @Html.Partial("_List_Pager")
        </p>
    </div>
</div>