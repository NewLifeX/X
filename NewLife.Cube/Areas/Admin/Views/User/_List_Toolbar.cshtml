@using NewLife;
@using NewLife.Web;
@using XCode;
@{
    var fact = ViewBag.Factory as IEntityOperate;
    var page = ViewBag.Page as Pager;
}
<div class="tableTools-container">
    <div class="form-inline">
        <form action="@Url.Action("index")@Html.Raw("?" + page.GetBaseUrl(false, true, true)) " method="post" role="form">
            @if (ManageProvider.User.Has(PermissionFlags.Insert))
            {
                @Html.ActionLink("添加" + ViewContext.Controller.GetType().GetDisplayName(), "Add", null, new { @class = "btn btn-success btn-sm" })
            }
            <div class="form-group">
                <label for="RoleID" class="control-label">角色：</label>
                @Html.DropDownList("RoleID", new SelectList(Role.FindAllWithCache().ToDictionary(), "Key", "Value", Request["RoleID"]), "全部", new { @class = "multiselect", onchange = "$(':submit').click();" })
            </div>
            <div class="form-group">
                <label for="dtStart" class="control-label">时间：</label>
                <input name="dtStart" type="date" id="dtStart" dateformat="yyyy-MM-dd" class="form-control form_datetime" />
            </div>
            <div class="form-group">
                <label for="dtEnd" class="control-label">至</label>
                <input name="dtEnd" type="date" id="dtEnd" dateformat="yyyy-MM-dd" class="form-control form_datetime" />
            </div>
            <div class="pull-right form-group">
                <div class="input-group">
                    <span class="input-group-addon">
                        <i class="ace-icon fa fa-check"></i>
                    </span>
                    <input name="q" type="search" id="q" value="@Request["q"]" class="form-control" placeholder="搜索关键字" />
                    <span class="input-group-btn">
                        <button type="submit" class="btn btn-purple btn-sm">
                            <span class="ace-icon fa fa-search icon-on-right bigger-110"></span>
                            查询
                        </button>
                    </span>
                </div>
            </div>
        </form>
    </div>
</div>
