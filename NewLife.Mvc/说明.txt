核心工作原理：
	1，本组件仅提供一个Mvc接口框架，不保证具有可供直接使用的实现。
	2，路由处理器Route实现了IHttpModule，作为总入口
	3，Route遍历控制器工厂IControllerFactory，直到找到一个支持处理当前Http请求的工厂为止
	4，因为可能存在动态目录路径，无法通过配置来指定由哪个控制器工厂来处理当前请求，而工厂内部可能通过读取数据库来判断是否支持
	5，由工厂创建一个控制器IController，Route把控制权转交给控制器

2011-11-01
1，调用模版引擎生成页面时，可以把控制器作为参数传递过去